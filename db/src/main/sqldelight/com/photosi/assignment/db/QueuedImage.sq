import com.photosi.assignment.db.QueuedImageStatus;

CREATE TABLE QueuedImage (
    id BLOB PRIMARY KEY,
    fileName TEXT NOT NULL,
    status INTEGER AS QueuedImageStatus
);

insert:
INSERT INTO QueuedImage(id, fileName)
VALUES ?;

selectAll:
SELECT * FROM QueuedImage;